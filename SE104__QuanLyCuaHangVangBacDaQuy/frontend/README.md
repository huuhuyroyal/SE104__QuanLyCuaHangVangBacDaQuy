# Frontend — Setup & Run

Copy `.env.example` to `.env` and set `VITE_API_URL` if needed.

1. Install dependencies
   npm install

2. Start dev server
   npm run dev

Default API base URL is `http://localhost:8080`. If your backend runs on a different host/port, update `VITE_API_URL`.
